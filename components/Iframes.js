function Iframes(elem){	if(!elem) return;	var iframeSrc = elem.getAttribute("data-src"),		iframeLinks = iframeSrc.split(", ");	elem.insertAdjacentHTML("afterbegin", this.getTemplate(iframeLinks));	}Iframes.prototype.getTemplate = function(src){	var template = "";	for (var i = 0; i < src.length; i++){		template += '<div class="val-outer-frame">' +						'<span class="val-ico-online">' +							'<i class="Online"></i>' +						'</span>' +						'<iframe width="100%" height="270px" src="' + src[i] + '"></iframe>' +					'</div>';	}	return template;};